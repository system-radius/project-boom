package com.radius.system.events.listeners;

import com.radius.system.events.parameters.FirePathEvent;

public interface FirePathListener {

    public void OnFirePathTrigger(FirePathEvent event);

}
